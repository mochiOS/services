[package]
id = "org.mochios.compositor"
name = "Compositor Service"
version = "1"
vendor = "mochiOS"

[[binary]]
path = "/system/services/compositor.service"
kind = "service"
requires = [
    "display.read",
    "fs.read.all",
    "input.keyboard",
    "input.pointer",
    "ipc.client",
    "ipc.server",
]
