[service]
id = "org.mochios.display"
name = "Display Driver"
version = "1"
entry = "display.driver"

[capabilities]
requires = [
    "device.gpu",
    "display.read",
    "dma.allocate",
    "ipc.client",
    "ipc.server",
]
