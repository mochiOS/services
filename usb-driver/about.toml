[driver]
id = "com.mochios.usb.qemu"
name = "QEMU USB Controller Driver"
version = "0.1.0"
description = "User-space USB controller enumerator for QEMU."
developer = "mochiOS"
entry = "entry.elf"

[plugkit]
api = "1"
driver_class = "usb"

[[match]]
bus = "pci"
class = "usb"

[capabilities]
requires = [
    "usb.access",
]

[provides]
interfaces = [ "usb.controller.enumeration" ]
